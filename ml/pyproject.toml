[project]
name = "ml-engine"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "pythermalcomfort>=2.7.3",
    "numba>=0.59.0",
    "numpy>=1.26.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"