fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy[asyncio]==2.0.25
aiosqlite==0.20.0
asyncpg==0.30.0
celery==5.3.6
redis==5.3.1
pydantic-settings==2.3.4
pydantic==2.7.0
pythermalcomfort==3.8.0
aiohttp==3.9.1
# Needed by ml/risk_model.py at import time (pandas, joblib) and scikit-learn
# for the Isolation Forest it loads. Pinned to the same versions the model was
# trained with (ml/requirements.txt) — a mismatch raises
# InconsistentVersionWarning and risks invalid predictions.
pandas==3.0.6
joblib==1.6.0
scikit-learn==1.9.1
numpy==2.5.3
python-dotenv==1.0.0
httpx==0.26.0
pytest==9.1.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
